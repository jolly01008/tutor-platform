<nav class="navbar navbar-expand-lg bg-body-tertiary px-5 py-2 bg-dark" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
      <h3><strong>Tutor</strong></h3>
    </a>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        {{#if user.isTeacher}}
        <li class="nav-item">
          <a class="nav-link active" href="/teacher/{{user.id}}">{{ user.name }}</a>
        </li>
        {{else}}
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/users/{{user.id}}">{{ user.name }}</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/users/{{user.id}}/apply">成為老師</a>
        </li>
        {{/if}}

      </ul>
    </div>
    <a href="/logout">
      <button type="button" class="btn btn-outline-light">logout</button>
    </a>
  </div>
</nav>